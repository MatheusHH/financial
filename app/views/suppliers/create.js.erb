 $(function () {
  <% if @supplier.errors.any? %>
	alert("Erros(s): <%= j @supplier.errors.full_messages.join('; ') %>")
  <% else %>
	alert(" Supplier created");
	$('#newCategoryModal').modal('hide');
    window.location.reload();
  <% end %>
});